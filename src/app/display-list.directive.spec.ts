import { DisplayListDirective } from './display-list.directive';

describe('DisplayListDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayListDirective();
    expect(directive).toBeTruthy();
  });
});
